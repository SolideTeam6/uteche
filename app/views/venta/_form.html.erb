<%= form_for(@ventum) do |f| %>
  <% if @ventum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ventum.errors.count, "error") %> prohibited this ventum from being saved:</h2>

      <ul>
      <% @ventum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
      <%= f.label 'Alumno' %><br><%= collection_select(:ventum,:student_id, @students, :id, :nombre, {prompt: false}) %>
  </div>
  <div class="field">
    <%= f.label 'Producto' %><br><%= collection_select(:ventum,:product_id, @products, :id, :nombre, {prompt: false,class:"col-md-2 control-label"}) %>
  </div>
  <div class="field">
    <%= f.label :cantidad %><br>
    <%= f.number_field :cantidad %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
